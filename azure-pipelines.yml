resources:
  repositories:
  - repository: common
    type: github
    name: test
    ref: 
    endpoint: github
    trigger: none
    pr:
      branches:
        include: main
      paths:
        include: test/amol

pool:
  vmImage: windows-latest

steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'

